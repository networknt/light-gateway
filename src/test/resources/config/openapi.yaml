openapi: 3.0.0
info:
    version: 1.0.0
    title: reverse proxy demo backend service
    description: reverse proxy backend with get, post endpoints
    contact:
        email: stevehu@gmail.com
    license:
        name: Apache 2.0
        url: http://www.apache.org/licenses/LICENSE-2.0.html
servers:
    - url: http://proxy.networknt.com

paths:
    /get:
        get:
            description: Return a JSON object
            operationId: getDataByGet
            responses:
                default:
                    description: Default response
    /post:
        post:
            description: Return a JSON object
            operationId: getDataByPost
            responses:
                default:
                    description: Default response
